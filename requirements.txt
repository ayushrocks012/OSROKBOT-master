# OSROKBOT canonical runtime dependencies.
# Target runtime: Python 3.13 on Windows.
# Keep this as the only requirements file; do not add nested requirements files.

# Image and tensor runtime for screenshots, YOLO perception, OCR, and memory.
opencv-python==4.13.0.92
numpy==2.4.4
Pillow==12.2.0

# PyQt desktop control overlay.
PyQt5==5.15.11
PyQt5-Qt5==5.15.2
PyQt5_sip==12.18.0

# Background capture and foreground window activation.
pywin32==311
PyGetWindow==0.0.9

# Foreground hardware input and emergency hotkey support.
interception-python==1.13.6
keyboard==0.13.5

# OCR and local configuration.
pytesseract==0.3.10
packaging==24.2
python-dotenv==1.0.0
pydantic==2.13.1

# Lyceum answer matching.
scikit-learn==1.8.0

# OpenAI Responses API fallback for Lyceum and advisory vision recovery.
openai==2.31.0
jiter==0.14.0

# Optional YOLO object detection when ROK_YOLO_WEIGHTS is configured.
ultralytics==8.4.24

# Optional guarded AI planner perception and local vector memory.
easyocr==1.7.2
sentence-transformers==3.4.1
faiss-cpu==1.9.0.post1

# Console diagnostics.
termcolor==2.4.0

# Developer QA tooling.
pytest==8.3.4
ruff==0.8.6
mypy==1.14.1
vulture==2.14
