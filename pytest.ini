[pytest]
testpaths = tests
pythonpath = Classes
addopts =
    -q
    --strict-markers
    --cov=ai_fallback
    --cov=ai_recovery_executor
    --cov=config_manager
    --cov=context
    --cov=dynamic_planner
    --cov=health_check
    --cov=maintainer_run
    --cov=model_manager
    --cov=OS_ROKBOT
    --cov=planner_decision_policy
    --cov=run_handoff
    --cov=runtime_composition
    --cov=runtime_journal
    --cov=security_utils
    --cov=state_machine
    --cov-report=term-missing:skip-covered
    --cov-fail-under=80
markers =
    integration: safe integration-style tests for OS and service seams; no live automation.
    supervised: opt-in tests that require an operator, foreground game window, or hardware input environment.
